# LLM (OpenAI 兼容 API) Configuration
# ┌─────────────────────────────────────────────────────────────────────────┐
# │ 本文件【可以完全不配置】：不配置/复制走也能正常启动和访问全部平台功能，  │
# │ 只有「AI 实验助手」需要模型——教师登录后点右侧 AI 面板顶部齿轮，          │
# │ 在网页「模型配置」里填 API Key 即可，保存即时生效、无需重启。            │
# └─────────────────────────────────────────────────────────────────────────┘
# 如需用 .env 方式：复制本文件为 .env 并填入真实值；.env 已被 .gitignore 排除。
# 网页保存的配置（llm_config.json）优先于 .env（网页字段留空才逐项回退 .env）。
# 任意 OpenAI 兼容服务商均可，常用组合示例：
#   OpenAI:   LLM_BASE_URL=https://api.openai.com/v1             LLM_MODEL=gpt-4o-mini
#   DeepSeek: LLM_BASE_URL=https://api.deepseek.com/v1           LLM_MODEL=deepseek-chat
#   Moonshot: LLM_BASE_URL=https://api.moonshot.cn/v1            LLM_MODEL=moonshot-v1-8k
#   智谱:     LLM_BASE_URL=https://open.bigmodel.cn/api/paas/v4  LLM_MODEL=glm-4
LLM_API_KEY=your_api_key_here
LLM_BASE_URL=https://api.openai.com/v1
LLM_MODEL=gpt-4o-mini
